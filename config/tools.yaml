config_version: 0.3.1
tools:
- name: llm_query
  type: builtin
  enabled: true
  config:
    temperature: 0.2
    max_tokens: null
- name: translator
  type: builtin
  enabled: true
  config:
    api_provider: llm
    target_lang: Chinese
- name: calculator
  type: builtin
  enabled: true
  config:
    precision: 10
memory:
  enabled: false
